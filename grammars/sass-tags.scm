(rule_set
  (selectors) @name) @definition.selector

(mixin_statement
  (name) @name) @definition.mixin

(function_statement
  (name) @name) @definition.function

(keyframes_statement
  (keyframes_name) @name) @definition.keyframes
